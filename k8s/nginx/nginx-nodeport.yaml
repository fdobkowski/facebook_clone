apiVersion: v1
kind: Service
metadata:
  name: my-nginx-nodeport
  labels:
    app: my-nginx
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31000
  selector:
    app: my-nginx
    type: frontend